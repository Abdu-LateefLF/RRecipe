<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes</title>

    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script src="/js/bootstrap.min.js"></script>
</head>

<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <h1>View Recipe: <%= recipe.name %></h1>
        <h5>Prep Time: <%= recipe.preparationTime %> min(s)</h3>

        <div class="row mt-3">
            <div class="col-4">
                <label for="description">Description:</label>
                <p class="border border-1" id="description"><%= recipe.briefDescription %></p>
                <div>
                    <label for="ingredient">Ingredients:</label>
                    <ul class="list-group">
                        <% for (let ing of recipe.ingredients) {%>
                            <li class="list-group-item"><%= ing %></li>
                        <% } %>
                      </ul>
                </div>
            </div>
            <div class="col-4">
                <label for="instructions">Instructions:</label>
                <p class="border border-1" id="instructions"><%= recipe.instructions %></p>
            </div>
        </div>

        <form action="/recipes/<%= recipe._id %>?_method=Delete" method="post">
            <div class="mt-3">
                <a href="/recipes/<%= recipe._id %>/edit" class="btn btn-primary">Edit</a>
                <a href="/recipes" class="btn btn-secondary">Back</a>
                <button class="btn btn-danger">Delete</button>
            </div>
        </form>
    </div>

</body>
</html>